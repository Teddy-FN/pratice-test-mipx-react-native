import {onLogin} from '../store/signIn/action';
import {onCategory} from '../store/category/action';
import {onProducts} from '../store/product/actions';
export const actions = {
  onLogin,
  onCategory,
  onProducts,
};
